Primary keys:
UUID

Foreign keys:
same UUID type as referenced PK

created_at:
TIMESTAMPTZ

updated_at:
TIMESTAMPTZ

calendar-only date:
DATE

money:
NUMERIC / DECIMAL

flexible structured config:
JSONB